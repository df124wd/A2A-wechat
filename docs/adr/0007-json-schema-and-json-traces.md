# JSON Schema and JSON traces

The protocol contract should be expressed with JSON Schema, and message and trace artifacts should remain JSON. This keeps the communication records inspectable by humans, easy for the TypeScript UI to read, and straightforward for the Python experiment runtime to validate.
