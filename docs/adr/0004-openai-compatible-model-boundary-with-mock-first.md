# OpenAI-compatible model boundary with mock first

The project should define a lightweight model boundary shaped around an OpenAI-compatible interface while initially providing a mock model for protocol experiments. This keeps the agent communication work unblocked by provider credentials, network access, or model cost, while leaving a realistic path to attach an actual provider later.
