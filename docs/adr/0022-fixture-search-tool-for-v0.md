# Fixture search tool for v0

The v0 Search Tool should read local JSON fixtures instead of calling a real search API. This keeps the Research-to-Write scenario deterministic enough for schema validation and UI development while still exposing the research agent to realistic search-result shape, including multiple sources, ranking, URLs, and snippets.
