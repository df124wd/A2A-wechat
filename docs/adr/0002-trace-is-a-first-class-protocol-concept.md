# Trace is a first-class protocol concept

Every message in the project must be eligible for inclusion in a trace. This is intentional because the project studies communication between agents and tools; without an inspectable trace, the protocol cannot be evaluated beyond final outputs.
