# Curated traces and file upload for UI

The v0 UI should include curated trace examples and also support loading trace JSON by file upload or drag and drop. This lets the UI show useful data immediately while still allowing users to inspect traces generated by the CLI, without requiring a backend API in the first version.
