# Trace contains participant snapshot

Each v0 trace should include the participants involved in that run, including their stable IDs and declared capabilities. This keeps trace files self-contained so the TypeScript UI can inspect a trace without loading a separate participant registry.
