# Trace loader and timeline first UI

The v0 UI should open directly on trace loading and timeline inspection, with a message inspector showing details for the selected message. The UI observes traces rather than running experiments, keeping the first interface focused on communication visibility instead of becoming a scenario control surface.
