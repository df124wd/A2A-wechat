# Protocol first, then framework comparison

This project starts by defining and exercising a minimal communication protocol before implementing the same scenarios with an existing agent framework. We choose this order so the project can study what agent frameworks reveal, hide, or constrain in agent-to-agent and agent-to-tool communication instead of becoming only a framework API exercise.
