# Python experiments with TypeScript UI

The project should use Python for running agent communication experiments and TypeScript for the trace observation UI. This adds early complexity compared with a single-stack TypeScript project, but reflects a realistic split between AI experiment code and web-based inspection tools.
