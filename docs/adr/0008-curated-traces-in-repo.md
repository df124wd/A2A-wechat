# Curated traces in repo

The repository should keep a small set of curated JSON trace examples for tests and UI development, while ignoring ordinary local experiment output. This gives the protocol and trace UI stable fixtures without letting routine runs pollute the repository.
