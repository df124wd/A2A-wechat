# v0 success standard

The first successful version is a schema-validated Research-to-Write trace generated by the Python CLI with a mock model and inspectable in the TypeScript trace UI. It must include a human task, planner delegation, research tool call and result, and writer response, but it does not require a real LLM, real search, or deterministic replay.
