# CLI experiments with minimal trace UI

The first implementation should run protocol experiments from a CLI and provide a minimal web UI for reading traces through a timeline and message inspector. This keeps protocol iteration fast while still making the communication process observable, without turning the first phase into a full application UI project.
