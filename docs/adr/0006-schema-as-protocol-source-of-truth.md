# Schema as protocol source of truth

The protocol contract should be defined as language-neutral schema rather than owned by either the Python experiment runtime or the TypeScript UI. This prevents the two stacks from drifting and keeps the communication protocol itself as the center of the project.
